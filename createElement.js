// Create an input tag and append it to the body
let newElement = document.createElement('input') //Creates an input tag
document.body.appendChild(newElement) //Appends the created input element in the body
